ALGORITMO
    DECLARE NOME TIPO LITERAL
	DECLARE SALBASE, TEMPO, PERCIMPTO, VALIMPTO, VALGRAT,
		VALAUX, SALLIQ TIPO NUMERICO

	ESCREVA "DIGITE O NOME"
	LEIA NOME
	ESCREVA "DIGITE O SALARIO BASE"
	LEIA SALBASE
	ESCREVA "DIGITE O TEMPO DE TRABALHO"
	LEIA TEMPO
	SE SALBASE <= 200 ENTAO INICIO
		PERCIMPTO = 0
	FIM
	SENAO INICIO
		SE SALBASE > 200 E SALBASE <= 450 ENTAO INICIO
			PERCIMPTO = 3
		FIM
		SENAO INICIO
			SE SALBASE > 450 E SALBASE < 700 ENTAO INICIO
				PERCIMPTO = 8
			FIM
			SENAO INICIO
				SE SALBASE = 700 ENTAO INICIO
				    PERCIMPTO = 10
				FIM
				SENAO INICIO
					SE SALBASE > 700 ENTÃO INICIO
						PERCIMPTO = 12
					FIM
				FIM
			FIM
		FIM
	FIM
	VALIMPTO = (SALBASE * PERCIMPTO) / 100
	ESCREVA "O VALOR DO IMPOSTO E" , VALIMPTO

	SE SALBASE > 500 ENTAO INICIO
		SE TEMPO <= 3 ENTAO INICIO
			VALGRAT = ((SALBASE * 2) / 100) + 50
		FIM
		SENAO INICIO
			VALGRAT = ((SALBASE * 3) / 100) + 60
		FIM
	FIM
	SENAO INICIO
		SE TEMPO <= 3 ENTAO INICIO
			VALGRAT = ((SALBASE * 5) / 100) + 23
		FIM
		SENAO INICIO
			SE TEMPO > 3 E TEMPO < 6 ENTAO INICIO
				VALGRAT = ((SALBASE * 6) / 100) + 35
			FIM
			SENAO INICIO
				VALGRAT = ((SALBASE * 10) / 100) + 33
			FIM
		FIM
	FIM
	ESCREVA "O VALOR DA GRATIFICACAO E", VALGRAT
	SE TEMPO <= 10 ENTAO INICIO
		VALAUX = (SALBASE * 4) / 100
	FIM
	SENAO INICIO
		VALAUX = (SALBASE * 6) / 100
	FIM
	ESCREVA "O VALOR DO AUXILIO ALIMENTACAO E", VALAUX

	SALLIQ = (SALBASE + VALGRAT + VALAUX) - VALIMPTO
	ESCREVA "O VALOR DO SALARIO LIQUIDO E", SALLIQ

	ESCREVA "E PARA NAO ESQUECER, SEU NOME E", NOME

FIM_ALGORITMO









